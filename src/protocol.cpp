#include <protocol.h>

Brain::Brain(int INPUT_Q, int LAYER_Q, int OUTPUT_Q) {
  this.INPUT_Q = INPUT_Q;
  this.LAYER_Q = LAYER_Q;
  this.OUTPUT_Q = OUTPUT_Q;

  this.regenerate_structure();
  return 0;
}

Brain::regenerate_structure() {
  for() {
    
  };
}

